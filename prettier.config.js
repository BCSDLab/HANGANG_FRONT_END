module.exports = {
  tabWidth: 2, //  tab width must be 2
  printWidth: 120, //  set print width on 120 letters
  bracketSpacing: true, // {foo:bar} => { foo: bar }
  jsxBracketSameLine: false, // don't put '>' at the end of the last line
  arrowParens: "always", // always include parens on arrow function parameter
};
